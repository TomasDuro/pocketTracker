#ifndef TRAINED_RESULTS_H
#define TRAINED_RESULTS_H

#include <stdio.h>
#include <iostream>

void loadA(float* theta);
void loadB(float* theta);
void loadC(float* theta);
void loadD(float* theta);

#endif
